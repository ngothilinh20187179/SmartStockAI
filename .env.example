# --- DATABASE CONFIGURATION ---
DB_USER=admin
DB_PASSWORD=my_very_secure_password_123
DB_NAME=smartstock_db
DB_PORT=5432

# Run backend in Docker: db
# Run backend locally: localhost
DB_HOST=db
DATABASE_URL=postgresql://%(DB_USER)s:%(DB_PASSWORD)s@%(DB_HOST)s:%(DB_PORT)s/%(DB_NAME)s